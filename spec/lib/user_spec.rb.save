require 'spec_helper'
require 'user'

describe User do
    before :each do
    	@user = User.new
    end

    it "expect user to have no negative points" do
	expect(@user).to_not be_player_points
    end
    
    it "expects user to not have negative amount of points" do
	expect(@user).to_not     
    end

    it "expect user to not be a captain" do
	expect(@user).to be_is_captain
    end

    it "expect user to not be in a team" do
        expect(@user).to be_is_in_team
    end
end
